# Посчитать четные и нечетные цифры введенного натурального числа.
# Например, если введено число 34560, в нем 3 четные цифры (4, 6 и 0) и 2 нечетные (3 и 5).


a = input('Введите число: ')
even = 0
odd = 0

for numeric in a:

    if int(numeric) % 2 == 0:
        even += 1

    else:
        odd += 1

print(f'В числе четных цифр: {even}, нечетных цифр: {odd}')

# Найти сумму n элементов следующего ряда чисел: 1, -0.5, 0.25, -0.125,…
# Количество элементов (n) вводится с клавиатуры.

n = int(input('Введите количество чисел в ряду (число): '))
n_i = 1
summa = 0

for i in range(n):
    summa += n_i
    n_i /= -2

print(f'Сумма чисел равна: {summa}')

# Посчитать, сколько раз встречается определенная цифра в введенной последовательности чисел.
# Количество вводимых чисел и цифра, которую необходимо посчитать, задаются вводом с клавиатуры.

number_find = int(input('Введите цифру : '))
n = int(input('Введите количество чисел : '))
result = 0

for i in range(n):
    number = input('Введите  число: ')

    for num in number:
        if int(num) == number_find:
            result += 1

print(f'Количество входов по всем числам: {result}')

